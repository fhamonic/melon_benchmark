// the generated NETGEN family (scripts/generate_netgen.py):
// the structure where a source-packed arc order costs the block
// rule the most, and the one LEMON's arc mixing is built for.
#include "netgen_instances.hpp"

#include "dimacs_min_body.hpp"
