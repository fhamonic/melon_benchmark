// k x k assignment (scripts/generate_mcf_families.py): unit
// supplies and unit capacities, so the pivot count is tiny next
// to the arc count and setup cost dominates the run.
#include "assignment_instances.hpp"

#include "dimacs_min_body.hpp"
