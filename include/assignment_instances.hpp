#pragma once

// Generated by scripts/generate_mcf_families.py -- do not
// edit; the (path, |V|, |A|) triples are written from the
// actual generated graphs, so they cannot drift from the
// data.

#include <filesystem>
#include <tuple>
#include <vector>

std::vector<std::tuple<std::filesystem::path, int, int>> instances = {
    {"data/assignment/assignment_k200.min", 400, 40000},
    {"data/assignment/assignment_k400.min", 800, 160000},
    {"data/assignment/assignment_k700.min", 1400, 490000}};
